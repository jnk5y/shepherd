version: "3"

services:
  app:
    image: jnk5y/shepherd
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      placement:
        constraints:
        - node.role == manager
    environment:
      TZ: 'America/New_York'
    restart: unless-stopped
